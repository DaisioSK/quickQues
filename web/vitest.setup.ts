/**
 * Global setup file pulled in by vitest.config.ts -> test.setupFiles.
 *
 * Imports `@testing-library/jest-dom/vitest` so matchers like
 * `expect(el).toBeInTheDocument()` resolve in every test without each
 * file repeating the import.
 */

import "@testing-library/jest-dom/vitest";
